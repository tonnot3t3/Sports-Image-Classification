[pytest]
testpaths = tests
addopts = -ra -q --strict-markers
# Tests in this suite are sync; `strict` mode prevents pytest-asyncio
# from auto-wrapping them and surfacing odd collection errors.
asyncio_mode = strict
pythonpath = .
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
